# data/

Aquí va `etiquetas_categoria.csv` (Fase 1 del plan): una muestra de reportes reales/sintéticos etiquetados a mano con `reporte`, `categoria_real`, `subtipo_real`, usada para medir el accuracy del grafo — no para entrenar nada.
